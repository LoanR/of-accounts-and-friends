<% publish_to @path do %>
  var newFriendNum = <%= @current_board.friends.count %> + 1;
  $('#friends_board_<%= @new_friend.board.id %>').append("<%= j render(@new_friend) %>");
  $('.friend:nth-child(' + newFriendNum + ')').hide().slideDown();

  changeBoardAverage(<%= @current_board.id %>, <%= @current_board.get_friends_ids %>);
  friendsBalance(<%= @current_board.id %>, <%= @current_board.get_friends_ids %>);
<% end %>
